spring:
  application:
    name: seingeurieSpring

  datasource:
    url: jdbc:postgresql://localhost:5432/db_seigneurie
    username: postgres
    password: azerty+35
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
#        format_sql: true
#        default_schema: public